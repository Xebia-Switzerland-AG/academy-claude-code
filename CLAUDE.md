# CLAUDE.md

See [README.md](README.md) for setup, usage, and input format.

## Important: sample data

`sample_data/q1_display_with_issues.csv` is **intentionally malformed** (zero-spend rows, CTR mismatches). Do not fix it — it exists to test the validation warning paths.
